node {
    stage('Build') {
        // Run build commands
        echo "Building project shivani..."
    }
    stage('Test') {
        // Run tests
        echo "Running tests jamwal..."
    }
    stage('Deploy') {
        echo "Deploying application bhatia..."
    }
}
