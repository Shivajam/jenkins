node {
    stage('Build') {
        // Run build commands
        echo "Building project..."
    }
    stage('Test') {
        // Run tests
        echo "Running tests..."
    }
    stage('Deploy') {
        echo "Deploying application..."
    }
}
